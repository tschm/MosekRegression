name: "book"

on:
- push

jobs:
  test:
    runs-on: "ubuntu-latest"
    steps:
      - uses: cvxgrp/.github/actions/uv/coverage@v2.1.1
        with:
          source-folder: src/mosek_tools
          tests-folder: src/tests
          coveralls: 'false'

  jupyter:
    runs-on: "ubuntu-latest"
    steps:
      - uses: cvxgrp/.github/actions/uv/jupyter@v2.1.1

  pdoc:
    runs-on: "ubuntu-latest"
    steps:
      - uses: cvxgrp/.github/actions/uv/pdoc@v2.1.1
        with:
          source-folder: src/mosek_tools

  book:
    runs-on: "ubuntu-latest"
    needs: [test, jupyter, pdoc]

    permissions:
      contents: write

    steps:
      - uses: cvxgrp/.github/actions/book@v2.1.1
